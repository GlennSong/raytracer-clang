# Capability ledger

The step `docs/knowledge-retention-plan.md` asked for: before building anything
that overlaps an existing capability, write down what exists and why it is not
being used. One row per capability; the "used by" column is the audit trail.

## Roads (opened for ADR-0083; settled by ADR-0085 — two generators, selected per entity)

| Capability | Where | lanelab | Why / why not |
|---|---|---|---|
| Road topology `RoadGraph` (nodes, edges, class, layer, spec) | `procgen/city/road_network.h` | input model target | lanelab's graph JSON resolves to spines + `RoadSpec`; a converter to `RoadGraph` is the integration step |
| Lane model `RoadSpec` / `RoadBand` (direction per lane) | `procgen/city/road_spec.h` | **used** | the lane layout lanelab expands is expressed as bands, not a new model |
| Freeway aux lanes `LaneSchedule` | `procgen/city/alignment.h` | not used | lanelab's pockets/aux are lane births and deaths on any road; same idea, general form |
| Swept-lattice mesher + full-ring junction patch | `procgen/city/road_lattice.h`, `road_net.cpp:498` | not used | **both ship** (ADR-0085): the lattice meshes every `shape:"road"` level and stays the mesher of record; lanelab meshes `shape:"lanelab"`. A level picks. |
| In-house polygon offset / union / hole bridging | `procgen/city/road_offset.h` | not used | needs difference, closing offsets and outer/hole trees; Clipper2 behind `lanelab/geom2d.cpp`; substitutable if ADR-0083 point 3 is refused |
| Ear-clip triangulation (mapbox port) | `procgen/city/triangulate.h` | not used | fans long slivers that sag under curved profiles; CDT ladders instead |
| Joint vertical alignment `weldChainProfiles` (ADR-0082) | `procgen/city/road_mesh.h` | not yet | lanelab has node + crossing consistency with iteration; the joint solve is the integration target |
| Terrain conform `roadNetConformRegions` → `TerrainFlatten` | `procgen/city/road_net.h:181` | export path | the lab nudges a heightfield grid; integration emits flatten regions |
| 2.5-D crossings `planarizeLayered` | `procgen/city/road_network.h:298` | equivalent | lanelab decides levels per triangle by height cluster; no 2-D planarisation |
| Winding rule `MeshBuilder::emitTri/UV` | `engine/mesh_builder.h` | **used** | all lanelab triangles go through it |
| Heightfield functor `HeightField` | `procgen/terrain_field.h` | **used** | lanelab's procedural terrain is one of these |
| `proto/roadlab` (removed) | history, Aug 11–17 | not carried | clean-room, no engine types; its OpenDRIVE round-trip idea is noted, not ported |


## Caches, bundles and export (opened for ADR-0084)

| Capability | Where | Used by | Why / why not |
|---|---|---|---|
| Erosion cache `cache/terrain/<hash>.bin` | `procgen/terrain_field.cpp:618` | terrain load | the precedent: FNV-1a over sampled inputs + params + tag, raw floats, `RT_NOCACHE` |
| Cloud-noise cache `cache/clouds/<hash>.bin` | `renderer/metal/cloud_noise.h:265` | sky | same shape, descriptor string with a version tag |
| Level bundle `Bundle`/`BundleWriter` | `engine/bundle/bundle.h` | loader, `rt_bake`, editor | one file, aligned mmap-able sections, JSON TOC, manifest as commit marker; general — no producer knowledge |
| Producers, keys, bake, obtain, prune | `engine/bundle/bake.h` | loader, `rt_bake`, editor | content keys + code tags, copy-forward of unchanged producers, `RT_NOCACHE`/`RT_BUNDLE_*` knobs; `pruneBundles` keeps the newest per level |
| Codecs `PackedMesh`, height grid, road graphs, rings | `engine/bundle/codecs.h` | city producer, GLB view | float32 packed meshes; the cold path packs too (byte-identical cold/warm) |
| City producer | `procgen/lanelab/city_producer.h` | loader, `rt_bake`, editor | lanelab's products per render cell; `kLanelabBuildTag` |
| Lots producer | `procgen/lanelab/lots_producer.h` | loader, `rt_bake`, editor | the lot pass on the city's blocks, read back through `BundleWriter::readBack`; `kLotsBuildTag`; lab levels only |
| City lots producer `citylots` | `procgen/city/citylots_producer.h` | loader, `rt_bake`, editor | the lattice city's lot pre-pass (`shape:"road"` + `generate`), engine_core, registers unconditionally; `kCityLotsBuildTag`. Piedmont 77 s -> 28 s warm |
| Lot pre-pass world `CityPrePass` | `engine/city_prepass.h` | `citylots` producer | ground, road nets, carve, earthwork, ROW from the level JSON alone; refuses a level shaped by a Lua script or a corridor solve rather than approximating it |
| Lot codecs | `procgen/city/lot_cache.h` | lots producer, loader | `NetLotResult` to sections — every `BuildingParams` field by hand, parts per render cell, `kLotsFormatVersion` |
| Lot-pass setup | `engine/lot_grow_setup.h` | loader `growCityLots`, lots producer | one derivation of `LotParams`/`EdgeBlockParams`/books/spawn from a level |
| Build identity `build_info.h` | generated by CMake | logs, `info`, manifests | `git describe --dirty` at configure |
| GLB writer | `engine/glb_export.h` (was lanelab-only) | `lanelab_tool`, bundle view | TANGENT + COLOR_0 + extras added on the move |
| Bundle → GLB view | `engine/bundle/bundle_glb.h` | `rt_bake --glb`, editor | per material or per cell from section names alone |
| `MeshBuilder::chunkByCell` | `engine/mesh_builder.h` (was `level_loader.cpp`) | loader chunking, city producer | a bundle cell is a render cell |


## Buildings (opened for skyscrapers v2, 2026-09-13 — site plans, envelopes, cores)

| Capability | Where | Used by | Why / why not |
|---|---|---|---|
| Skyline census `SkylineCensus` / `skylineCensus` | `procgen/city/city_lots.h` | lot pass report, loader warm paths (lanelab + citylots), `test_city_lots` | storeys histogram, towers >20/40/60, tallest, footprint rectilinearity, open space by kind, per district — computed from `LotBuilding` records so a warm load (which grows nothing) reports the same numbers as a grow |
| Shared facade truth `FacadeLayout` | `procgen/city/shape_grammar.cpp:520` | full, flat (LOD1) and inner-wall emitters | the model for site plans and cores: one derivation, several consumers, so LODs and interiors agree by construction |
| Storey stack `storeyPlans` on `massStack` | `procgen/city/shape_grammar.h` | exterior massing, `growInterior`, `test_city` | the tiers a building is built from — one plan per tier and its first floor — from `(plan, params)` alone; `Envelope::None` = the old uniform offsets, `StreetWallSetback` = the 1916 New York base / setback / steps / shaft |
| Lot-level massings `Massing::{LotPlan,RectYard,Circle,RowStrip,Plaza,PodiumTower,Park}` | `procgen/city/city_lots.cpp:2297-2655` | lot pass | the only places a building pulls in from the lot line; `PodiumTower` is ad-hoc code replaced by the podium envelope |
| Plaza kit `sculptPlaza`, park kit `sculptPark`, yard kit `sculptYard` | `procgen/city/city_lots.cpp:484-926` | lot pass | paving, stairs at street mouths, fountain, planters, benches, paths, hedges — the dressing every open-space kind reuses |
| Enterable interiors (ADR-0080) `growInterior`, `interiorLayout`, `BuildingInteriorSystem`, `DoorSystem` | `procgen/city/shape_grammar.h:168-233`, `engine/systems/building_interior_system.h`, `door_system.h` | every walk-in building; tall ones when a core fits | one straight stair up to three floors; from four floors the core (below) — `growInterior(plan, params, baseY, collider, k0, k1)` streams a storey window |
| The core `CorePlan` / `coreFor` / `emitCoreStorey` (ADR-0086 point 11) | `procgen/city/core_plan.h` | ground ceiling holes, `interiorLayout`, `wantsDoorway`, `BuildingInteriorSystem`, `ElevatorSystem`, `test_core_plan` | two dog-leg stairwells + a hoistway bank + a service block, seated in the top tier facing the entrance; one derivation for the exterior, the streamed interior and the runtime |
| Elevators `ElevatorSystem` | `engine/systems/elevator_system.h` | the building the player is in | one kinematic cab per hoistway + sliding hoistway-door leaves around the player; `E` calls / goes, Up/Down picks; ImGui panel; headless `step()` |
| Walker `CharacterVirtual` step-up / step-down / ride | `engine/physics/physics_world.cpp:437-537`, `systems/player_system.h` | player, pedestrians | stairs and kerbs; the ground branch adds `GetGroundVelocity()` so a kinematic mover carries the walker (`character_rides_a_kinematic_platform`) |
| Night gate `NightGlow` + `duskRamp` | `engine/components.h:395`, `vehicle_lamps.h:74` | lit windows, lamp shells, interiors | one emission per Renderable, ramped on the dusk curve |
| Emissive vertex tint `RenderMaterial::FLAG_EMISSIVE_VERTEX_TINT` | `renderer/renderer.h`; `shaders/{vulkan/mesh.frag, metal/lighting_surface.metal, webgpu/mesh.wgsl}` | lit glass (`materialFor(PartId::GlassLit)`), `litTint` | the vertex colour multiplies the emission instead of the albedo: many tints in one part, day look untouched |
| Beacon flash `BeaconBlink` + `beaconBlinkGate` + `beaconCellPhase` | `engine/components.h`, `vehicle_lamps.h`, `systems/day_night_system.cpp` (`applyBeaconBlink`), `PartId::{Beacon,BeaconGlow,BeaconHaze}` | tall-roof aviation lamps (loader tags the 24 m beacon chunks) | dusk ramp × a per-cell flash cycle written per frame, divided by the night exposure adaptation, scaling a translucent halo's opacity and fading it past 20-35 m; the pattern for any other flashing or glowing night light |
| Beacon tiers `BeaconLightSystem` | `engine/systems/beacon_light_system.h`; `BuildingRecord::beacons` | every aviation lamp in the city | far: one instanced billboard group with a baked glow texture; near: the nearest lamps as red point lights (`lighting.beaconPoints`); the model for sprite + light LODs on any small light |
| Light LOD block `CitySimConfig::light{SpriteIn,SphereOut,Radius,Range,Count}`, `lamp{LightRadius,LightCount,LightRange,GlowDistance}` | `engine/components.h`; level JSON `citysim` | beacon tiers; the street lamps' point lights (render system merge), glow shells (loader) and far sprites (`BeaconLightSystem`) | level-authored beside `detailDistance`; every small light's tiers read these |
| Lit dressing `PartId::LitBand` | `procgen/city/shape_grammar.cpp` (the roof/night block) | crown bands, signage boxes, podium uplight bands | emissive with the vertex tint, no room behind it; the part for any lit surface that is not a window |
| Weather policy `DayNightConfig::weather` + `weatherForDay` | `engine/components.h`; level JSON `dayNight.weather`; `engine/weather_cycle.h`; `systems/day_night_system.cpp` | every level with a volumetric deck (auto by default) | "auto" picks today's state from the seed and the day of year (clear 30 %, fair 42 %, overcast 20 %, storm 8 %) and lets the 4-hour walk run; "off" keeps the authored cloud block; a kind name pins it |
| Cloud march: multiple scattering, base ambient, 4×4 upsample | `shaders/vulkan/clouds.frag`, `clouds_composite.frag` (Metal mirrored, uncompiled) | the volumetric deck | three-octave scattering so bases read grey not black; the 2×2 dither's stipple averaged out by a tent upsample; stride cap 6× against mid-distance shelves |
| Lighting spec `BuildingParams::{crown,signage,uplights}` | `procgen/city/shape_grammar.h`; Lua `crown`, `signage`, `uplights` (`scripting/procgen_bindings.cpp`) | style/archetype books | author a tower's night dressing per recipe; absent = the hash decides |
| Distant-tier lit windows `bakeLitWindowMap` + `appendLotMassBox` UVs | `engine/level_loader.cpp`; `procgen/city/city_lots.h` (`kMassBoxCell`, `kMassBoxTile`) | HLOD proxies past `detailDistance` | one baked emissive window grid on the mass boxes under `NightGlow`; the far city glows after dusk |
| Texture alpha in the transparent pass `RenderMaterial::FLAG_ALPHA_FROM_MAP` | `renderer/renderer.h`; `shaders/{vulkan/mesh.frag, metal/lighting_surface.metal}` | beacon sprites | opacity × the albedo map's alpha per fragment: soft-edged billboards and decals without a new pipeline |
| Front face only `RenderMaterial::FLAG_FRONT_ONLY` | `renderer/renderer.h`; `shaders/{vulkan/mesh.frag, metal/lighting_surface.metal}` | exterior window panes (`materialFor(Glass/GlassLit)`) | the back of a surface does not exist: from inside a streamed floor the facade pane vanishes and the interior's clear pane shows the city |
| Debug spawn `RT_SPAWN=x,y,z` | `engine/level_loader.cpp` | headless interior frames, tests | the walker starts anywhere, verbatim — a tower floor, say — and the interior streams around it |
| Interior mapping `RenderMaterial::FLAG_INTERIOR_MAP` + `bakeRoomAtlas` | `renderer/renderer.h`; `shaders/vulkan/mesh.frag`; `engine/level_loader.cpp` | lit panes (`PartId::GlassLit`) in every facade tier | a virtual room behind a flat pane with parallax, from a baked 2×2 room atlas in the albedo slot; the faked tier of the windows plan; Metal owed |
| Clear lobby glass `PartId::GlassClear` | `procgen/city/shape_grammar.cpp` (`appendGlassParts`, the ground-storey loop) | enterable buildings' ground storeys at Full detail | the real tier: outer and inner panes transparent, the lobby visible from the street |
| Static instancing `InstanceGroup` | `engine/components.h:152` | vegetation, lamps, signals | candidate for repeated typical floors and beacon shells; instances are static, no per-instance culling |

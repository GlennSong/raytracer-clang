# Piedmont — project memory

The accumulated, hard-won context for the piedmont city round: decisions,
phase status, root causes, and the traps that cost real time to find. Kept in
the repo (not only in the assistant's memory store) so any session — or any
person — can be pointed at it alongside `docs/vehicle-unification-plan.md`,
which holds the forward plan.

Read this for WHY things are the way they are. Read the plan for WHAT to do
next. Entries are roughly chronological; the most recent rounds are richest.

---

The approved culmination round: **piedmont** — 8km×8km world, city (+900,+900) + two towns (east +3050,+400 oldtown; south +300,+3050 residential), NO freeways (backbone MST reused as **Arterial**-class connectors — `backboneClass` param needed, metro.cpp:431 hardcodes Freeway), big blocks (growth-time spacing primary: block 220 / kill_radius 110 / seg 40 / loops 300-650; min_road_len 150 cleanup only), full districts→lots→buildings with plinth 0.45, three-tier traffic (P: 12 Jolt cars @90m — exists; K: kinematic bubble 500m/620m; V: NEW persistent ~1Hz coarse tick, 60 uid-buckets, 3000 drivers + 1800 peds), L-shaped range west+north (rangeSpine + range ridge object, rangeHeight 420, variation 0.7).

Plan file: `~/.claude/plans/here-s-what-i-d-like-tingly-pebble.md` (full phase specs P0–P6 + gates + verification matrix).

**Decisions:** driving is the bar (viewer drive city→town + editor review); 30fps first cut; GPU erosion bake = the "compute where it pays" pick; cinematic sky phase (Hillaire LUTs + wire the half-built volumetric `clouds.metal` SLAB mode — it exists, never in the Metal concat list); instance caps init-time configurable per level (`"render"` block in level JSON), not raised globally.

**Phase status (2026-07-25):** P0 COMPLETE: P0.1 farPlane (view_distance.h) · P0.2 configurable caps + loud overflow · P0.3 Tracy zones · P0.4 async CDLOD (refinement-gated, byte-identical, JobSystem; RT_SYNC_CDLOD=1 fallback) · P0.5 GPU erosion (bit-deterministic fixed-point atomics; **8km bake res2048/3M droplets = 1.08s**; RT_CPU_EROSION=1 fallback; cache tag "gpu-erosion-v1") · P0.6 collider window budgeted (1 build/fixed-step + under-feet exemption + 1.2s velocity prefetch) + clock backlog telemetry; camera-lerp hypothesis parked pending a real drive. P1 level authored. P2 core landed (probe-validated: 1 component, towns reached, 0 spans <120m, ~91 blocks median 32.6k m²).

**P2 hard-won semantics (do not relearn):** (1) "min road length" must be measured on JUNCTION-TO-JUNCTION SPANS walked through degree-2 curve nodes — colonization arterials are chains of ~seg-length curve segments, so raw-edge min_road_len 150 shreds geometry; keep min_road_len=30 (stub cleanup) and use the NEW `min_block_edge` (150) which drives (a) the fabric-fill cell floor at 1.35× headroom (fill cells at exactly the min get eaten by consolidation — faces collapse), (b) loop-attachment thinning at 1.35×, (c) `consolidateJunctionSpans` (road_constraints.cpp): fuse when arms fit ≤4, DELETE only if detour ≤2 spans (junction-hops! node-hops walk around whole blocks and merge faces), else LENGTHEN along the chord. (2) Skeleton consolidates INSIDE buildMetro before fabric fill. (3) loopMin must be ≈1.35×min_block_edge (300 blocked closing to adjacent strands ~kill-radius apart). (4) Tuned piedmont recipe: seg 50/kill 110/merge 80/ambient 14/influence 560/loops 200-650/block 220/min_block_edge 150. (5) `backbone: "arterial"` JSON key avoids the RoadClass::Freeway hardcode (metro.cpp legacy backbone).

**20FPS FIXED + SOUTH HOLES FIXED (2026-07-26):** (1) Glenn's 20fps: RT_DUMP_STATS instrument chain (frame stats → fixedUpdate split → per-system bill via typeid) found **CityWalkerSystem at 10.1 of 13.0 ms/step** — it spawned a Jolt capsule + articulated body for ALL 1800 peds at build (pre-tiering design) with O(walkers×people) separation. Fix: per-step reconcile, bodies for K-TIER PEDS ONLY (explicit pw.removeCharacter — physics has no entity-destroy hook). Result 10.1→0.49ms, fixed step 13.0→5.0ms (in 60Hz budget; death spiral gone), play-mode ~23→~44fps at spawn. Remaining known per-frame costs if 60fps round wants them: CityRenderSystem fixed 3.7ms (tier-center world scan + bake), ~15ms update-phase citysim vs edit mode (per-system update instrument added but its lines never printed in --play — possibly a derived state overrides update(); investigate then). (2) South-exit holes: acute-pair wedge (two w17 arterials 26° apart at hub) → dissolveAcuteArms (deletion-only, sliver-length ≤250m + 3-span detour proof; ribbon-overlap criterion tried and FALSIFIED). Both drives CLEAN; foldback 56.6→28.4°, ratchet tightened to Glenn's 35°. Frontage now an ENGINE guarantee: lot >26m from road surface → green (landmarks exempt).

**P7.1 COMMITTED (40deb9b) + NEW DIRECTION APPROVED (2026-07-26 evening):** fabric variants in-engine (chords/bisect/court, mix dispatch), REGIONAL density unlock (consolidateJunctionSpans position-dependent floor overload; fabric_core_len 110; sandbox 10/12 faces subdivide, 297 blocks), road_map_svg diagnostic tool (level pipeline → layered SVG; links libengine_core; no SVG lib needed). GLENN'S MAP CRITIQUE drove the pivot: arterials tangled/no breadth (hub ring packs 9 in r1400, no min separation), city too small (must be ≥4×4km on a 12×12 WORLD — 8×8 too tight for city+approach roads+towns), crammed tiny lots (parcel fallback on unfabric'd faces). APPROVED PLAN (plan file top): **P7.3 CITY PLANNER first** — NATIVE QT dock panel (Glenn explicitly rejected ImGui) + EditorBridge planner service: graph-only regen (~1s, no mesh — uploadMesh leak!), baked ribbon overlays (city_render:707 pattern; DebugDraw is wrong tool + not registered in editor), orbit-ortho camera presets Top/Iso (fly-ortho hardcoded 10m = unusable; camera_system.h needs orbitController accessor; 3 hardcoded-perspective overlay bugs at editor_system.cpp:670/704/1188), typed input works in Qt. THEN P7.2 12×12 rebuild THROUGH the tool: Stage A0 terrain 12000, Stage A Poisson hubs (min sep ~1.1km, count from packing) + §10.6 through-route chaining reused from corridor branch + corridors only along chained routes + arterial_span key ~700, Stage B fabric on real faces, Stage C frontage-ring parceling. **PHASE 1 SHIPPED (e755b51):** Qt dock "City Planner" (under Inspector) + EditorBridge planner API (attachPlanner/plannerRecipe/plannerApplyRecipe→stats/plannerBakeMesh/plannerSetLayer/plannerCameraPreset); engine side src/engine/city_planner.{h,cpp}; graph-only regen ~1s; FLAG_OVERLAY ribbon layers (hubs/arterials/nodes) rebuilt on regen only; orbit-ortho presets (Top pitch +89 — orbit convention is +up; Iso +35.264); SIX hardcoded-perspective editor overlay sites fixed via cameraProjection helper (incl. ImGuizmo ortho flags); Bake via revision-keyed acquireMesh (swap not leak; legacy regenerateRoad leak untouched). Overlays appear after first Regenerate press (not on load — cheap follow-up). arterial_span key: panel writes it, generator DOESN'T consume yet (P7.2 Stage A). Offscreen Qt tests in editor_qt_tests. GLENN'S FLOW: open editor_app → piedmont_roads → City Planner dock → [Top] → tweak Bones → [Regenerate] → judge → [Bake mesh].

**(prior P7 v3 record):** Glenn's FINAL design (supersedes two-pass colonization): PATCH-CONFORMING fabric — rings parallel to arterials + perpendicular ribs, Manhattan-in-the-boundary-frame, wedges/seams = wanted variety. Full plan in plan file ACTIVE section (P7 v3): fabric LAB first (Glenn picks rings+ribs vs tensor BY EYE — build fabric_lab.json with oval/banana/acute/tiny panels + hybrid), road hierarchy freeway→alley (access-control ordering; boulevard=median via medianProfile; alley preset exists), canonical CHAIN stationing (adjacent patches share station nodes — never split twice), offset-T signals (alternate sides → mostly unsignalized 3-ways, 4-ways at collector spacing), rim rings + sprawl pods for open edges (retire edgeBlocks fakery), streetscape policy table per district (green waves/vistas/plazas/town roundabouts — domain+rhythm+seeded, planned-not-rolled), roundabout INTEGRATION CONTRACT (clearance precondition, graph-first, neighborhood planarize, island=park lot — fixes Glenn's bystander-road overlap bug). Architect gates: 110m core is ILLEGAL until region-aware consolidation lands (Phase 0 = clamp 150 first); nasty fixtures FIRST. **DONE: patch_fabric.{h,cpp} in engine_core — insetRingArcs medial-validity primitive PROBE-VERIFIED (oval 86 segs; banana 4 arcs@40/empty@55; tiny→0). GLENN SAW THE SVG PREVIEW (round 5 feedback): tensor demo produces strands not cells (keep only if lab redeems); ring middle was empty (Phase-4 leftover pass not shown) — he LIKES the empty-center-as-park version → ROUND-6 VERDICTS on SVG previews: KEEP ring court; DISCARD ring-filled/gridworks/tensor. NEW PRIMARY = MESH PATCH (Glenn's construction): boundary stations as visible VERTICES on the arterials (= the Step 2.5 canonical chain stations), streets traced INWARD along boundary normals, laterals at equal depths close quads by construction, traces stop at the medial. Lab variants: MESH PATCH + RING COURT, district-weighted. ROUND 7 FINAL: true patch = N-SIDED (corners=junctions, sides=stationed chains); PRIMARY = fabricCoonsPatch — transfinite interpolation (same math as road_lattice coonsPatch), N=4 direct, N!=4 via midpoint decomposition to quad sub-patches; complete-by-construction (no dangles/empty middle); ring court = same construction with interior rows omitted; paving as fallback. ROUND 8: crossings=vertices. ROUND 9 FINAL: CHORD-STRAIGHTENING (bilinear blend rejected — rough/forced): each street = straight chord between ITS OWN two stations (endpoints fixed on arterials, natural slope kept); vertices = chord crossings; ROUND 10 DIALS: conform 0..1 / pairing (orthogonal<->matched-fan) / station jitter / side matching (equal<->free counts) / relax passes / angle snap — districts = presets on the six dials, patch seed nudges within. ROUND 11 (degenerate/open strategies, SVG-approved): mismatched side counts NEVER degenerate — dense-side extras T-TERMINATE at first crossing (city form, not defect); counts within 1.3x snap equal; sub-block/sliver → single block or ring court. N!=4/concave → midpoint decomposition to quad sub-patches (seams = real streets); concave cut at reflex first. Open rim → COMB strips + rim-ring closure + sprawl pods. ROUND 12: (a) THIRD VARIANT fabricBisect — Glenn's recursive bisection (= existing buildDistrict/parcel recursive-OBB scheme PROMOTED to street scale): cuts near side midpoints, diagonal/bent allowed, cut line stationed with y nodes seeding further bisection; goal = interesting topology variety, not correctness; oldtown/industrial preset. Menu now: chords (planned city) / bisect (grown city) / ring court (green heart). (b) Unclosed comb ends = CUL-DE-SACS with turning bulbs, marked intentional (exempt from dangle rule); suburban rim form. (c) Undivided awkward patches (triangles/slivers) = single MEGA-LOT, seeded district-weighted use table: office park+parking / big-box / park / school campus / plaza — recipes office_park & parking_garage exist; NEW parking-lot sculpt + the table. ROUND 13 (bisect node hygiene): recursive bisection clusters nodes → TWO-THRESHOLD collapse: HARD floor ~40-50m (always fuse — 'crosswalk with no road to cross' scale; = consolidateJunctionSpans with small floor), SOFT band up to district span floor fuses with SEEDED PROBABILITY p per node-pair hash (deterministic 'random chance'; per-district p — oldtown ~0.5 keeps character, residential ~0.9). Signals/crosswalks self-cure: they derive from topology (≥4 approaches / crossable frontage), so collapsing nodes removes the furniture automatically. NEXT: implement fabricCoonsPatch (conformDepth + ring-court switches) as the PRIMARY generator + keep ring court; drop ring-filled/gridworks/tensor code paths from the lab; "fabric_demo" recipe kind, fabric_lab.json (mesh patch + ring court + banana/acute/tiny fixtures), test_fabric.cpp, real-mesh captures → Glenn's binding pick.**

**(superseded v2 for record):** Dense downtown via a SECOND space-colonization pass over the finished arterial graph, per density region (core disc ~110–130m blocks / midtown ring ~160–200 / edges+towns skip), seeded from arterial nodes inside each region, attractors only within it, street/collector class, planarize welds the crossings — enclosure EMERGES (metropolis proved fine-scale settings: seg 32/kill 70). Cleanup tail runs per-region with graded span floors (110/150/200). Replaces my rings/ladders version — reuses the trusted generator twice at proven scales instead of inventing geometry; P2.5's chaos lesson argues FOR the split. Gates: per-ring span histograms, blocks ≥100 (baseline 33), quads ≥50%, foldback ≤35°, town drives clean, determinism. Parcel/massing gradient rides along (downtown targetArea ~350, buildChance 0.97). Full design in plan file P7. Baseline: 60 spans mean 316/median 259m, 33 blocks, 19km. BUILD NEXT SESSION.

**DRIVE-FEEDBACK ROUND 2 (2026-07-26, commits f53ccab..eb3ccff):** (1) RUBBER-BAND TRUE ROOT CAUSE (supersedes the P0.6 budget/prefetch story): while InVehicle the on-foot character parks at the kerb, PlayerSystem stops moving it, and TerrainLodSystem's collider window centred on THAT — the car outran its own physics floor ~90m from the entry point. Window now follows the vehicle when seated. Glenn's next drive verdicts it. (2) Bloom is level-authored (environment.bloom; piedmont 3.0/0.3/0.05) and the debug overlay NO LONGER persists bloom to settings.json (stale saved sliders were silently stomping every authored value — same rule as lighting). (3) NEW-CAR FLEET UNLOCKED: citysim's vehicle VM was bare, vehicles.lua died on require, box-fleet fallback since the fleet landed; VM now layers openProcgenLibrary+openModuleLoader. (4) Sky: circumsolar aureole in scatteringSkyRadiance (2-lobe, transmittance-tinted, clamped under half-float — unclamped it rendered a BLACK sun) + cloud aerial fade by march ENTRY distance (piedmont clouds.far 9000) killed the horizon white wall. (5) Piedmont spawns downtown on foot + parked drivable sedan 12m away. FOLLOW-UP GLENN WANTS: Perlin-Worley 3D noise for clouds (billow instead of value-noise potatoes) + edge erosion, then height-profile/weather-map cloud types.

**ACCEPTANCE SUITE LANDED (2026-07-26):** Drive gate (test_piedmont_drive.cpp): city→east **CLEAN over 2.3km** (0 holes/steps/grades/blocked — the automated "drivable city→town" bar); city→south clean EXCEPT the first ~150m at the city-centre hub exit (907,915): 24 hole samples, ratcheted ≤30 with target 0 — **OPEN DEFECT, first P6 task**: the south route's first nav links traverse something the mesher never sweeps (east from the same start is clean; suspect a nav-only link class/knot-merge artifact — print route.links[0..2] klass/walkable to localize). Frontage gate (Haiku agent): 190 buildings, median 6.5m, max 24.1m from a road, bound 30m. Five-stage contract file test_city_stages.cpp encodes Glenn's pipeline. RSS 2.3GB. Remaining P6: fix south-exit holes, street-level captures, camera bookmarks, fog tuning, Glenn's manual drive + 30fps HUD.

**P3 VISUAL GATE PASSED (2026-07-25, commit 5d4ec43 + verification):** piedmont.json builds in **12s** (caches warm), **RSS 2.3GB** (budget 10GB — memory gate passed), **4218 entities**, road mesh sane, no overflow warnings. The frame shows the full composition working: tower core → brick midrise ring → house fabric, buildings FRONTING streets in rows, mid-block greens, volumetric clouds + range horizon. Capture: fly camera via settings + `--edit` (citysim play state owns the camera). REMAINING FOR P6: RSS number (time -l got killed pre-print — rerun letting the viewer exit cleanly), street-level captures, camera bookmark sidecars, drive gates (drive_probe city→town + Glenn's manual drive = rubber-band + foldback verdict), 30fps HUD check, fog/haze tuning (ground wash strong), enclosure density + 35° ratchet + frontage/stage-contract tests (P3 leftovers, agent-delegable — Haiku fine for the mechanical test work, keep road-generator design changes on a strong model: the generator is deterministic-chaotic).

**P2.5 LANDED (2026-07-25, commit 1f3d67f — Glenn's drive feedback round):** Foldbacks + missing grid fabric. ANSWERS GIVEN TO GLENN: the 5-stage pipeline (hotspots→colonization arterials→fabric grids→districts→frontage lots→building-in-lot) IS codified and running; the metro path just never ran bend-relax (district-only), and my min_block_edge floor starved gridFill. FIX: big-block metros colonize at ARTERIAL scale (seg 120/kill 300/influence 800 — geometry born right), per-district big-block cell tables (150-240m, crisp crook-0 grids outside oldtown), 2-round dedup→consolidate→merge→relaxSharpBends tail (relax LAST; junctions pinned). Audited gates print numbers: worst deflection 56.6° (ratchet 60°, TARGET 35° — wire `cutSharpCorners`, landed UNWIRED after its v1 minted parallel pairs + a disconnect), fabric 33 blocks/24% quads (TARGET ≥50% — enclosure density follow-up: more arterial loops; naive stride/hopBar boost backfired, needs deliberate ring templates per site). REMAINING for P3 resume: frontage gate in test_city_lots, test_city_stages five-stage contract file, per-hub landmark quotas, piedmont.json (P3 agent died on Fable budget mid-file — resumable). CAUTION: this generator is deterministic-chaotic — tiny pass-order changes cascade (dedup-before-vs-after changed block counts 2×); measure through test_metro_sites' printed audits, not ad-hoc probes.

**P4 TRAFFIC LANDED (2026-07-25, commit 214bedc):** Tier V persistent far agents (60 uid-buckets ~1Hz, promote 500m/demote 620m at exact lane pose — zero teleports measured, V-vs-K travel truth 2%), uniform 64m agent grid replacing the linear scans (order-identical supersets — every pre-P4 ratchet bit-identical), uid-keyed incremental kinematic proxies. **3000 drivers + 1800 peds: 23.3ms → 1.45ms/step.** Opt-in: `citysim.tiered` (piedmont.json sets it). `make test` re-linked via RT_NO_GPU_EROSION stub gate.

**VISUAL GATE PASSED (2026-07-25):** piedmont_roads renders correctly — organic big-block network, both towns + connectors, L-range with varying peaks. Battery: `tools/piedmont_shots.sh` (runs `--edit`; a citysim level's PLAY state owns the camera every frame and ignores fly settings — THE root cause of every "invisible roads" capture; RT_DUMP_DRAWS=1 draw audit proved the road entity was drawn all along). Look tuning open: ground fog heavy, roads read white at distance (sidewalk/curb dominance), NW range corner low (~69m), editor panel occludes top-left of captures.

**Debug lessons (2026-07-25, cost hours — do not repeat):** (1) The erosion content-hash cache sampled only 64 pseudo-random points — a ±1.4km ridge feature in the 8km domain evaded ALL of them, so a stale ridge-era field kept feeding the ROAD pre-pass (roads grew/draped against terrain that no longer existed, graph literally identical across terrain edits). Fixed with multi-scale 16×16 lattices at full/half/quarter extent in `erodeField` (terrain_field.cpp). RT_NOCACHE skips but never overwrites a stale entry. (2) A level without `"version": 1` fails silently — flat gray frames. (3) A `player` block in a plan-review level parks the play camera on the spawn — sandbox levels must not have one. (4) `showPlan` CityPlanDebug bands are huge overlay ribbons that can cover streets from altitude. (5) WIP shader edits by concurrent agents load at viewer LAUNCH from disk (concat list) — captures during agent work are contaminated; capture only from committed state. (6) Headless captures: use the worktree + `tools/piedmont_shots.sh` pattern (settings.json flyEye/cameraMode); verify camera took effect before trusting a frame.

**P8 FOOTPRINT-FIRST CITY (2026-07-26, commits 2b176c1..3cb1229 — Glenn's 4-step masterplan, APPROVED plan in plan file):** SPACE COLONIZATION RETIRED from the arterial tier (kept behind recipe key for old levels). Pipeline: F0 footprint polygon per site (city_footprint.{h,cpp}: grid flood fill on isBuildable, seed-anchored morphological opening kills skinny lobes, boundary-walk contour + DP + Chaikin + deflection-bounded smoothing ≤22°, inscribed-disc fallback; NO inset certificate on derived footprints — measured false-positive, grid opening IS the guarantee) → F1 rim gates (even arc, hash phase) + MST connector-gate pairing → F2 CONSTRUCTED skeleton (arterial_skeleton.{h,cpp}): rim arterial + spine (2 displaced legs smoothed as ONE polyline — per-leg smoothing kinks the center) + recursive bisection to district cells ~district_len, cuts = midpoint-displacement curves (buildable-biased, cell-clamped), GLOBAL JUNCTION REGISTRY (cross-sibling endpoint snap, gates 2×; fixed 64° consolidation kinks AND upgraded topology to through-avenues) → F3 tier-1 COLLECTOR fabric on district faces (chords-with-corners else bisect at district_len/3, 400-650) → street fabric unchanged. arterial_span FINALLY consumed (skeleton consolidation floor, capped 0.35·district_len). stop_after stepper: footprint|skeleton|collectors|"". Gates in test_footprint_skeleton.cpp: in-polygon spans median 1929/min 1157/0<400, foldback 21.4°, no interior dead ends, center ≤4°, hub contract, determinism, /tmp/skel_debug.svg dump. SANDBOX FLIPPED: piedmont_roads.json skeleton:"footprint" district_len 1200 → real map: 573 blocks, 2179 buildings, rim + crossing avenues + districts + both towns. Planner: Footprint overlay layer, [Clear roads], mode-aware Bones dials + [Build to] stage combo. P8-F(1) LANDED (8e3e386): footprint_wobble 0.12 (seeded 3/5/8-harmonic clip radius — circles only when terrain+wobble both silent; ratio gate 1.38); piedmont.json flipped (523 blocks/3715 buildings); spawn moved to new downtown crossing (1030,830-ish; road_map_svg prints spawn hint = junction nearest hub0); drive test re-pinned to footprint recipe — BOTH drives CLEAN (east 3.8km, south 2.6km, 0/0/0/0 — south-hole saga + east hairpin were growth-skeleton artifacts). Battery /tmp/p8_shots. OBSERVED for Glenn's verdict: downtown reads SPARSE (big empty faces, buildings hug streets — frontage working but density dials tuned for old block sizes; empty lens-face right of center). REMAINING: P8-G editable footprint (tolerant validation — NOT insetRingArcs), 12×12 A0 numbers, plannerCreateRoad, rim_road=false unsupported (warns).

**P8.2 PERF + THE VEHICLE/LUA ARCHITECTURE (a23704a..3139d0f; HANDOFF: docs/vehicle-unification-plan.md — READ IT FIRST next session):** Perf round found + fixed: city-wide parked cars drawn every frame (one bounding sphere per InstanceGroup = no partial frustum cull; ~4.4k cars x 1.9k tris; now pose-resolved once + 450m cull), pose bake running every fixed step when only the last is rendered (FrameContext gained fixedStepIndex/Count), jobs uniform over downtown-concentrated workplaces (now gravity-biased) and one synchronised commute (now 5 shift archetypes) -> 7->16 fps, and a REAL latent traffic bug (arrivals verge-parking inside neighbouring carriageways damming junctions, 168.5s->15.2s). Sim owns its cadence now (CitySim::setTickPeriod; localHz 30 enabled). GHOST LIGHTS: lamps read agentPose (the planner ghost) while bodies read physPose_ (the Jolt pose) — the ghost "legitimately runs ahead/behind", so lamps floated off. NEVER pass an agent index to agentPose() from a second call site: it advances the tilt low-pass and running it twice a tick re-breaks car_pose_probe_no_vibration_on_jagged_ground. **METHOD LESSON (cost most of a session):** a viewer number is a SYMPTOM until a deterministic headless harness reproduces it — I proposed three causes (staleness, rate-dependence, tethering) and ALL evaporated under tighter control; the tier system is correct and the real issue is that the bubble is a RADIUS so cost scales with downtown density. tools/piedmont_perf.sh (waits for the city, pins the camera, samples a SIM-HOUR window, prints census above fps) + tests/test_sim_rate_ab.cpp are the instruments; wall-clock comparisons are void. **GLENN'S ARCHITECTURE RULING:** C++ = capability/primitives, Lua = description/recipes ("we can't possibly describe everything in C++"); scripting is engine infrastructure NOT conditional compilation; vehicles are optional CONTENT (a city with no cars must build); mesh.car IS the vehicle (new vehicles = catalogue entries), LODs must be DECIMATIONS not re-authorings; delete all block-car geometry. Standing criticism: all P8 procgen (footprint, arterial skeleton, fabric variants) is C++ with string-dispatched enums where it should be Lua recipes.

**P8.1c/d CARS + PARKING (12e6b93, a55ede2, 7a3b4ea, 56ce67e, 4f6dc09):** (1) OLD CARS ROOT CAUSE: vehicles.lua's `vehicle.fleet` authored BOX bodies BY DESIGN ("the fancier low-poly car is a later task") while the player's car + car_lab used mesh.car — so "12/12 scripted bodies" was true AND still box cars. Fleet now built by mesh.car over vehicle_classes at "mid" LOD, fitted to kFleet's slot box via half_extent; the reader accepts an optional `body` Mesh (luaToMesh) beside box `parts`. Verdict line prints tris/car (box ~170, real ~1900) + LOG_ERROR under 100 tris. (2) STALE-BINARY TRAP (hit TWICE): Glenn saw wheels+lamps only = an old reader ignoring `body`. ALWAYS `cmake --build build -j8` (ALL targets) before telling him to look. Also: citysim does NOT run under --edit (traffic only in Play), so edit-mode probes show no fleet line at all. (3) WHEELS: I copied the drivable spec's `-h/2 + r + h156` — that is a SUSPENSION ATTACH point, not a resting centre → every wheel a ride-height high, mesh floor at the rocker, cars floated ~13cm (this was ALSO the "parked cars floating" report, since bay cars sit at ground + H/2). Fixed to `-h/2 + r`; wheels are now CYLINDERS (bake_height_color with equal low/high = flat tint, the vertex paint the mesh API lacks); body+wheels fitted together once. Gate: fleet_cars_stand_on_their_wheels. (4) PARKING: BandKind::Parking existed but generated streets got no spec (legacy shim = travel lanes only) → bays at a hardcoded inset, 7cm over the kerb. Now roadSpecStreetParking (SW|CB|P2.5|T3.5|T3.5|P2.5|CB|SW), band rides RoadEdge/NavLink parkOffset+parkWidth, bays gated on HAVING a band; street_width 11→12 across 6 levels. WIDTH AGREEMENT is the crux (lots/nav/mesher each consult width independently — proved 0m delta, 0 building verts in road). Gate: test_city_parking. Stall paint shares kRoadMarkLift (2cm) with mesher stripes (was 5cm = floating slab). GAPS: collector lanes 4.0m (set collector_width 12 for 3.5), band has no distinct surface in 3D, mesher still reads edge width not the band list, district-kind levels get no parking.

**P8.1 CITY-LIFE ROUND (05cc642, bf6ec7f, 7755acc, e127276 — Glenn's buildings verdict fixed at roots):** (1) TOWERS: LotParams::center was NEVER set by any loader → coreness 0 city-wide; both loaders now anchor at the kind-0 hub; ceilings raised (glass 55-62 @hub, +40% deco/stepped, NEW PodiumTower massing 12%fin/3%com). (2) PARCELS: parcelFrontage rewritten as BOUNDARY-WALK ring (any simple polygon, grain-preserving depth clamp, SAT backstop, interior = sculpted court) — piedmont 2176→6127 built lots, landlocked rejects 1102→3; frontage gate 26→14m + landmark exemption removed; [citylots] rejection ledger prints every build. (3) CARS: job-fallback scan starts at per-agent hash (was: entire fallback fleet on the FIRST routable job = one street); brain-forced-odd home pick hash-mixed (raw modulo reached only odd home indices); parking bays on Collectors + ≥9.0m too; GRIDLOCK ROOT CAUSE: arrivals on ~4.5m junction-knot links verge-parked INSIDE neighboring carriageways, damming junctions (168.5s all-stop → 15.2s via pushPoseClearOfLanes on idle + verge fallbacks). (4) EDITOR PARITY: level_scene now forwards hubs/hubRadius/parcel/center (editor grew a DIFFERENT city before). (5) CLOUDS: Perlin-Worley (128³ base + 32³ detail, disk-cached, clouds.detailStrength; polish item: grain in dense banks). (6) enter/exit vehicle = G / D-pad Up / NEW Y-Triangle. (7) FLEET VERDICT line every citysim build ([citysim] fleet: N/M scripted OR LOG_ERROR BOX CARS + reason) — Glenn must read HIS console line to crack old-cars/ghost-taillights (my probes show the new fleet loading). OPEN: Glenn's fleet line + taillight float, cloud grain, courtMinArea docs-only, ~8% heavy-clip fill-greens, acute-corner slivers, perf round, P8-G, 12×12.

**Why:** this is the culmination of roads v2/v2.1 + metro + lots + traffic work; everything verified by gates (drive_probe city→town, test_metro_sites, test_tier_v, pit/riser probes, 30fps HUD).

**How to apply:** execute phases in order, gates green before next; iterate roads in piedmont_roads.json (planOnly); buildings in piedmont.json. Related: [[road_unification_plan]], [[roads_v21_progress]], [[verify-by-driving]], [[verify-in-qt-editor]].
